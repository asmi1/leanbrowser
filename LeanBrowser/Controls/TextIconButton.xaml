<UserControl x:Class="LeanBrowser.Controls.TextIconButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="280">
    <Grid x:Name="bg">
        <Button x:Name="btn" Style="{StaticResource TextIconButtonStyle}">
            <DockPanel>
                <Image x:Name="image" Style="{StaticResource ButtonImageStyle}" Source="{StaticResource menuDrawingImage}" />
                <TextBlock x:Name="label" Style="{StaticResource ButtonTextBlockStyle}" Text="New window" />
                <TextBlock x:Name="tip" Style="{StaticResource ButtonTipStyle}" Text="" />
            </DockPanel>
        </Button>
    </Grid>
</UserControl>
